notifications:
  email: false

language: python
python:
    - "2.7"

sudo: false

addons:
  apt:
    packages:
      - libhdf5-serial-dev
      - netcdf-bin
      - libnetcdf-dev

before_install:
    # Setup anaconda
    - wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
    - chmod +x miniconda.sh
    - ./miniconda.sh -b
    - export PATH=/home/travis/miniconda/bin:$PATH
    - conda update --yes conda

install:
    - conda install --yes python=$TRAVIS_PYTHON_VERSION atlas numpy scipy cython netcdf4 pandas
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - python setup.py build_ext --inplace

script:
    - export PYTHONPATH=`pwd`:$PYTHONPATH
    - flake8 parcels
    - flake8 tests
    - python tests/grid_peninsula.py 20 10
    - py.test -v -s tests/
